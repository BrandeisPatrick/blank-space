{
  "name": "@blankspace/api",
  "version": "0.0.1",
  "type": "module",
  "main": "./src/index.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "deploy:aws": "npm run build && serverless deploy",
    "build": "echo 'Build step for Lambda packaging'"
  },
  "dependencies": {
    "@blankspace/core": "workspace:*",
    "@google/genai": "^1.34.0",
    "firebase-admin": "^13.5.0",
    "openai": "^6.5.0"
  },
  "devDependencies": {
    "serverless": "^3.38.0",
    "serverless-http": "^3.2.0"
  }
}
