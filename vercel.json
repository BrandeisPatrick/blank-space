{
  "version": 2,
  "buildCommand": "cd apps/studio && npx vite build --mode production",
  "outputDirectory": "apps/studio/dist",
  "installCommand": "npm install",
  "env": {
    "NODE_ENV": "production",
    "VITE_ENABLE_DEVELOPER_MODE": "false"
  }
}